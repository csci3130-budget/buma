#<IfModule mod_rewrite.c>
	#Enable rewrite mode
	RewriteEngine On
	
	#If the URL point to a file, direct to it
	#RewriteCond %{REQUEST_FILENAME} !-f
	
	#If the URL point to a folder, direct to it
	#RewriteCond %{REQUEST_FILENAME} !-d
	
	# All the URLs
	RewriteRule ^(login|create_account|forgot|home|add_expense|wish_list|statistics|new_account|add_budget|register|logout)[/]*$ index.php?file=$1&friendly_url=true
#</IfModule>
